use bevy::prelude::*;

/// Stub — unit selection is now integrated into the Preparation screen shop row.
pub struct UnitSelectionPlugin;

impl Plugin for UnitSelectionPlugin {
    fn build(&self, _app: &mut App) {}
}
